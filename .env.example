# Copy this file to .env and adjust values as needed.
# When true, the app is expected to run purely off the local JSON files
# in datasets/ instead of any live/external data source.
USE_MOCK_DATA=true
